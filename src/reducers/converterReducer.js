const initialState = {
  // ici l'état initial
};

function converterReducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

export default converterReducer;
